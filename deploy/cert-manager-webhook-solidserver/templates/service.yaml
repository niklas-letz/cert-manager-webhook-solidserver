apiVersion: v1
kind: Service
metadata:
  name: {{ include "solidserver-webhook.fullname" . }}
  namespace: {{ .Release.Namespace | quote }}
  labels:
    app: {{ include "solidserver-webhook.name" . }}
    chart: {{ include "solidserver-webhook.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: https
      protocol: TCP
      name: https
  selector:
    app: {{ include "solidserver-webhook.name" . }}
    release: {{ .Release.Name }}
